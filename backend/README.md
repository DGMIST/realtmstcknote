# Sticky Notes backend part

#### `npm run dev`

Starts the application in development. Make sure that frontend is running and\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
